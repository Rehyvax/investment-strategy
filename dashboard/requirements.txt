streamlit>=1.30.0
pandas>=2.0.0
plotly>=5.18.0
python-dateutil>=2.8.0

# Anthropic API — Phase 2B-2 narrative generator + chat ad-hoc.
# Optional: dashboard runs in deterministic mode when ANTHROPIC_API_KEY
# is not configured. Install ensures the SDK is available if/when the
# operator sets the key.
anthropic>=0.34.0
